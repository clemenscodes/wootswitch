[package]
name = "wootswitch"
version = "0.1.0"
edition = "2021"

[package.metadata.crane]
name = "wootswitch"

[dependencies]
hidapi = "2"
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
